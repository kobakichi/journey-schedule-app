{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://journey-schedule-app.onrender.com/api/$1"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
